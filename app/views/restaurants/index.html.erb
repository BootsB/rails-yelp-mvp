<h1 class="mt-3">Restaurants:</h1>
<div class="list-group mt-3">
  <% @restaurants.each do |restaurant| %>
    <%= link_to "#{restaurant.name}", restaurant_path(restaurant.id), class: "list-group-item list-group-item-action" %>
  <% end %>
</div>
<br>
